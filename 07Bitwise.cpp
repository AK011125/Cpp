#include<iostream>
using namespace std;
int main(){
    int a = 5;
    cout<<(a>>3)<<endl; // it means left shift by 3 (means /2/2/2)
    cout<<(a<<3); // it means right shift by 3 (means *2*2*2)
    return 0;
}